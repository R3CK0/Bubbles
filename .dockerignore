# Never send local secrets (vault, DB, session grants) into the build context.
data
.env
.git
*.log
.DS_Store
*.xlsx
**/node_modules
# host-side web build output — the image builds its own in the web-build stage
public
web/.vite
